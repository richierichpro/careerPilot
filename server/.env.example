# Copy this file to .env and fill in real values. Never commit .env.

# Anthropic API key — server-side only, never expose to web or extension clients.
ANTHROPIC_API_KEY=

# Port the API server listens on.
PORT=8787

# Path to the local SQLite database file.
DATABASE_PATH=./data/careerpilot.sqlite
